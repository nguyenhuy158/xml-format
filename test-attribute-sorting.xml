<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        <!-- Test 1: Attributes in non-alphabetical order -->
        <!-- With sortAttributes=true, these will be sorted alphabetically -->
        <record id="test_record_1" model="test.model" active="true" name="Test Name" context="test_context" priority="high">
            <field name="description">This record has unsorted attributes</field>
        </record>

        <!-- Test 2: Form view with many attributes -->
        <record id="view_partner_form" model="ir.ui.view">
            <field name="name">res.partner.form</field>
            <field name="model">res.partner</field>
            <field name="arch" type="xml">
                <form string="Partner" class="o_partner_form" create="true" edit="true" delete="false">
                    <sheet>
                        <field name="image" widget="image" class="oe_avatar" readonly="false"/>
                        <div class="oe_title">
                            <field name="name" placeholder="Name" required="true" string="Partner Name"/>
                        </div>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- Test 3: Tree view -->
        <record id="view_partner_tree" model="ir.ui.view">
            <field name="name">res.partner.tree</field>
            <field name="model">res.partner</field>
            <field name="arch" type="xml">
                <tree string="Partners" editable="bottom" create="true" delete="true">
                    <field name="name" string="Name" required="true"/>
                    <field name="email" widget="email" string="Email Address"/>
                    <field name="phone" widget="phone" string="Phone Number"/>
                </tree>
            </field>
        </record>
    </data>
</odoo>