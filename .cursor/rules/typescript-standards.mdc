---
globs: *.ts,*.tsx
description: TypeScript coding standards and best practices
---

# TypeScript Coding Standards

## Core TypeScript Guidelines
- Use TypeScript for all new code
- Follow functional programming principles where possible
- Use interfaces for data structures and type definitions
- Prefer immutable data (const, readonly)
- Use optional chaining (?.) and nullish coalescing (??) operators

## VSCode Extension Specific Guidelines
- Write clean, production-ready TypeScript code using the latest VSCode API
- Follow the official VSCode extension structure
- Keep activation lightweight and modular
- Use simple English (A2 level) and clear technical words only

## Code Organization
- Keep components small and focused
- Use interfaces for data structures
- Prefer functional programming patterns
- Maintain clear separation between formatting logic and configuration
- Use proper TypeScript types throughout

## Project Structure References
- Main extension entry: [src/extension.ts](mdc:src/extension.ts)
- XML formatter: [src/formatters/xmlFormatter.ts](mdc:src/formatters/xmlFormatter.ts)
- Formatter types: [src/formatters/types.ts](mdc:src/formatters/types.ts)
- Configuration utilities: [src/utils/config.ts](mdc:src/utils/config.ts)
- Test configuration: [src/test/testConfig.ts](mdc:src/test/testConfig.ts)